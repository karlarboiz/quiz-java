
import React from "react";
//  import { useParams } from "react-router";
const QuizDetails = () => {
    // const {quizIdTag, itemId} = useParams();
    <React.Fragment>

    </React.Fragment>
}

export default QuizDetails;

// async function fetchQuizDetailsHandler({ request, params }) {
//     const quizIdTag = params.quizIdTag;
//     const itemId = params.itemId;


// }